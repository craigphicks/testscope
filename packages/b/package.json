{
  "name": "@craigphicks/testscp-b",
  "version": "0.0.8",
  "description": "test of monorepo github packages",
  "main": "index.js",
  "files": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/craigphicks/testscope.git"
  },
  "author": "craig p hicks",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "typescript": "^4.5.2"
  },
  "peerDependencies": {
    "shx": "^0.3.3"
  },
  "scripts": {
    "build": "tsc",
    "pub": "yarn version --patch && yarn pack --filename package.tgz && yarn publish --tag $npm_package_version package.tgz && shx rm package.tgz"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  }
}
