{
  "name": "@craigphicks/testscp-b",
  "version": "0.0.9",
  "description": "test of monorepo github packages",
  "main": "index.js",
  "files": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/craigphicks/testscope.git"
  },
  "author": "craig p hicks",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "typescript": "^4.5.2"
  },
  "peerDependencies": {
    "shx": "^0.3.3"
  },
  "scripts": {
    "build": "tsc",
    "getver": "echo $npm_package_version",
    "pub1": "yarn version --patch",
    "pub2": "yarn pack --filename package.tgz",
    "pub3": "yarn publish --tag $npm_package_version --new-version \"$npm_package_version\" package.tgz",
    "pub4": "shx rm package.tgz",
    "pub": "yarn version --patch && yarn pack --filename package.tgz && yarn publish --tag $npm_package_version --new-version \"$npm_package_version\" package.tgz && shx rm package.tgz"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  }
}
